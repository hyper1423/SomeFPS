#ifndef DEFINED_TYPES_HPP
#define DEFINED_TYPES_HPP

#include <cstddef>
#include <type_traits>

// These types actually don't have any constraints, but do mean intents.
namespace NumeralTypes {
    using enumInt = int;
    using clampedFloat = float;
}

#endif